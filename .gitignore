node_modules/
lib/
*.tsbuildinfo
**/e2e-acl-report.txt
# Archived pre-refactor source; kept locally for reference, never released.
backup/
# Packed release tarballs are attached to GitHub releases, not committed.
*.tgz
